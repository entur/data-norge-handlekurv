package no.digdir.catalog_view_api.client
/*
import java.util.UUID
import org.springframework.stereotype.Component
import org.springframework.web.reactive.function.client.WebClient
import org.springframework.web.reactive.function.client.bodyToMono


@Component
abstract class AltinnInstanceClient {
    val urlLocalhost = "http://local.altinn.cloud"
    val urlStaging = "http://local.altinn.cloud"
    val urlProd = "http://local.altinn.cloud"
    val webClient = WebClient.create(urlLocalhost)

    val basepath = "/digdir/tilgangssoknad/instances"

    fun createInstance(id: UUID) {
        val kommando = "/create"

        return webClient.post().uri(basepath + kommando).bodyValue(requestData).retrieve().bodyToMono<>()
    }
}

data class*/
